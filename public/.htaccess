# SPA routing for React/Vite on Apache (cPanel)
Options -MultiViews
RewriteEngine On
# Serve files and directories directly if they exist
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
# Otherwise, fallback to index.html (relative path works in root or subfolders)
RewriteRule ^ index.html [L]